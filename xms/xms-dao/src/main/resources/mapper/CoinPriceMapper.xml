<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xms.dao.mapper.CoinPriceMapper">

    <resultMap type="CoinPrice" id="CoinPriceResult">
        <result property="id"    column="id"    />
        <result property="coinType"    column="coin_type"    />
        <result property="symbol"    column="symbol"    />
        <result property="initPrice"    column="init_price"    />
        <result property="currentPrice"    column="current_price"    />
        <result property="status"    column="status"    />
        <result property="remark"    column="remark"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectCoinPriceVo">
        select id, coin_type, symbol, init_price, current_price, status, remark, create_time, update_time from t_coin_price
    </sql>

    <select id="selectCoinPriceList" parameterType="CoinPrice"
            resultMap="CoinPriceResult">
        <include refid="selectCoinPriceVo"/>
        <where>
            <if test="coinType != null "> and coin_type = #{coinType}</if>
            <if test="symbol != null  and symbol != ''"> and symbol = #{symbol}</if>
            <if test="initPrice != null "> and init_price = #{initPrice}</if>
            <if test="currentPrice != null "> and current_price = #{currentPrice}</if>
            <if test="status != null "> and status = #{status}</if>
            <if test="createTime != null "> and create_time = #{createTime}</if>
        </where>
        order by id desc
    </select>

</mapper>
